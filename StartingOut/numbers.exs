
IO.puts("#{2+15}")

IO.puts("#{49*100}")

IO.puts("#{1892 - 1472}")

IO.puts("#{5 / 2}")

IO.puts("#{div(5,2)}")

IO.puts("#{rem(5,2)}")

IO.puts("#{(50 * 100) - 4999}")

IO.puts("#{-(50 * 100 - 4999)}")

IO.puts("#{-50 * (100 - 4999)}")

IO.puts("#{0b101010}")

IO.puts("#{0o0677}")

IO.puts("#{0xAE}")

System.halt(0)
